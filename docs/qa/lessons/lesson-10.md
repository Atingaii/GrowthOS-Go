# 第 10 节 QA 验收证据

- **日期：** 2026-08-29
- **产物：** [为什么第一版不直接微服务](../../course/part-02/lesson-10-modular-monolith-first.md)
- **结果：** 通过

## 验收范围

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 模块化单体选择有当前事实和方案比较 | 通过 | 第 10 节“当前证据”“方案比较” |
| 长期架构决策已登记 | 通过 | `ADR-0007` 与 ADR 索引 |
| 源码依赖方向和数据库所有权明确 | 通过 | 第 10 节“源码与依赖规则” |
| 微服务拆分具有可核查触发条件 | 通过 | 第 10 节“何时拆分” |
| 没有提前引入产品 API、RPC、表或中间件 | 通过 | Git 变更清单与第 10 节 API 记录 |
| 课程、QA、API 与 ADR 索引一致 | 通过 | `make doc-check` |
| 仓库统一质量门禁通过 | 通过 | `make verify` |

## 执行命令

```bash
go list ./...
make doc-check
make verify
git diff --check HEAD^
```

原始执行环境：macOS arm64、Go 1.26.6、Node.js 24.19.0、pnpm 11.22.0。第 10 节验收时仓库尚声明 Go 1.24.0；第 11 节根据 [ADR-0008](../../decisions/ADR-0008-supported-go-toolchain-baseline.md)把当前最低工具链统一为实际已验证的 Go 1.26.6。本行保留第 10 节当时真正使用的工具链，不表示用旧版本重新执行了验收。

结果：Go 包发现、单元测试、文档检查、TypeScript 类型检查和 Vite 生产构建全部通过。Vite 仍报告既有入口包大于 500 kB 的非阻塞提示，本节没有改变前端产物。

## 未覆盖项

- 当前没有产品运行时，模块依赖规则只能通过结构和评审验收；第 11 节开始加入可执行代码后补自动化依赖测试；
- 当前没有真实负载、独立发布或故障域数据，微服务触发条件将在第 73 节重新验证；
- 本机没有 `/mnt/e/TencentGo/growthOS` 个人 Vault，未执行该外部镜像同步；仓库文档事实源和同步工具未受影响。

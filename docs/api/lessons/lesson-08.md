# 第 8 节 API 记录：画 V0 系统设计

- **章节：** [画 V0 系统设计](../../course/part-01/lesson-08-v0-system-design.md)
- **日期：** 2026-08-09
- **状态：** 无 API 变化
- **QA：** [第 8 节 QA 验收](../../qa/lessons/lesson-08.md)

## 本节 API 变化

本节新增 V0 逻辑系统设计，没有新增、修改或删除真实 HTTP API、RPC、事件契约、MCP Tool 或 LLM 调用。`web/` 仍使用 Mock 数据。

架构图中的箭头表示业务协作或信息方向，不代表已经存在可调用接口，也不锁定同步 HTTP、gRPC 或消息队列。

## 未来契约边界

| 边界 | 后续契约必须表达 | 当前状态 |
| --- | --- | --- |
| Web → GrowthOS | 身份、请求/追踪标识、错误语义和业务状态 | 第 11、15 节开始 |
| 上下文 → 上下文 | 命令、查询、事实事件及权威所有者 | 按业务章节出现 |
| 外部身份/交易/渠道 → GrowthOS | 外部标识、来源时间、幂等和结果映射 | 未设计 |
| AI → Governance/业务 Tool | 参数 Schema、风险等级、审批、执行标识和结果查询 | 第 81 节后设计 |

## 前端影响

没有真实联调变化。V0 明确 React 前端最终会承载用户端、运营后台和 AI Operator，但页面不能直接访问数据库、外部 LLM 或跳过服务端 Governance。

## 后续章节输入

- 第 11 节登记第一个 `GET /health` 真实接口；
- 第 15 节记录 React 与 Go API 的首次联调契约；
- 每个业务 API 出现时再确定资源、字段、错误码、幂等和状态语义。

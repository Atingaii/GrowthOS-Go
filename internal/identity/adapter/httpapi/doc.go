// Package httpapi exposes the Lesson 32 same-origin workforce session HTTP
// contract. It orchestrates strict request decoding, Cookie, Origin, Fetch
// Metadata, CSRF, and Identity application use cases without owning credential,
// session, authorization, Role, Permission, Scope, Policy, tenant, or SQL facts.
package httpapi

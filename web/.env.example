# Node-side Vite proxy target. This value is not embedded into browser code.
# Keep credentials, database addresses, and other secrets out of every web env file.
GROWTHOS_WEB_API_PROXY_TARGET=http://127.0.0.1:8080
